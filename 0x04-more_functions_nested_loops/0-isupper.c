#include "main.h"

/**
 * isupper - checks for uppercase character
 * return: 1 if uppercase 0 if not
 */

int isupper(int c)
{
	if (c => 65 && c <= 90)
	{
		putchar('c');
		return (1);
	}
	else
	{
		putchar('c');
		return (0);
	}
	putchar('\n');
}
